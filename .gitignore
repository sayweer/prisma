# Rust's output directory
target

# Local settings
.soroban
.stellar

# Node / build / OS
node_modules
dist
.DS_Store
*.log
.env
.env.local

# ZK build artifacts (circuits/contracts)
build/
*.zkey
*.ptau
*.wtns
circuits/build/
